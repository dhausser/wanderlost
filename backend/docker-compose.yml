version: '3'
services:
  prisma:
    image: prismagraphql/prisma:1.34
    restart: always
    ports:
      - '4466:4466'
    environment:
      PRISMA_CONFIG: |
        port: 4466
        databases:
          default:
            connector: mongo
            uri: mongodb+srv://dhausser:lkDmDJbWHzlWZO8j@sick-fits-dsrre.mongodb.net/test?retryWrites=true&w=majority
  mongo:
    image: mongo:4.2.3
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: dhausser
      MONGO_INITDB_ROOT_PASSWORD: lkDmDJbWHzlWZO8j
    ports:
      - '27017:27017'
    volumes:
      - mongo:/var/lib/mongo
volumes:
  mongo: ~